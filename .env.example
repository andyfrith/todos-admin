# Database (required for app and migrations)
# Option A: single URL
DATABASE_URL=postgresql://user:password@host:port/dbname

# Option B: separate variables (used if DATABASE_URL is not set)
# DB_USER=postgres
# DB_PASSWORD=
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=postgres

# Copy this file to .env or .env.local and fill in real values.
# Do not commit .env or .env.local. For production (e.g. Cloudflare Workers),
# set variables in the dashboard or use: pnpm wrangler secret put DATABASE_URL
